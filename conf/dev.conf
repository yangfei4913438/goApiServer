# 开发环境配置
[dev]
# 运行端口
httpport = 8060

# http 请求
# 测试连接用的IP和端口，必须和下面完整URL的IP端口一致，否则没有意义
test_url = "127.0.0.1:8081"
# 实际使用的完整URL
base_url = "http://127.0.0.1:8081"

# 日志
log_filename = "logs/server.log"
log_level = 7
log_daily = true
log_maxdays = 7
log_maxlines = 100000
log_maxsize = 0

# redis配置
redis.host = 10.0.0.3:6379
redis.password = "yf@123456"
redis.db = 0
redis.maxidle = 100
redis.maxactive = 1000
redis.idletimeout = 600

# mysql配置
mysql_user = "root"
mysql_password = "Yf@111111"
mysql_net = "tcp"
mysql_host = "10.0.0.3"
mysql_port = "3306"
mysql_db = "test"
mysql_charset = "utf8mb4"
mysql_max_life_time = 300
mysql_max_open_conns = 1000
mysql_max_idle_conns = 20